import { Placeholder } from "@/components/Placeholder";
export default function Page() {
  return (
    <Placeholder
      title="Journal des appels"
      subtitle="Chaque appel pris par SWIPT, avec sa transcription intégrale et la fiche extraite."
      spec="SPEC §4.1 — module critique"
    />
  );
}
